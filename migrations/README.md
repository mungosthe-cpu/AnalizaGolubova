# Migrations

Ovaj folder sadrži SQL istoriju šeme:

- `0001_central_schema.sql` – centralna baza (`data/takmicari_grupacije.db`)
- `0002_track_schema.sql` – šema za staze/trke (`Staze/2025/*.db`)

Za svaku narednu promenu šeme dodati novi numerisani fajl:

- `0003_...sql`, `0004_...sql`, itd.
